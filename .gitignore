.DS_Store
.worktrees/
.claude/worktrees/

# Reference projects and old app snapshots used for analysis only.
explore/

# Runtime data produced by the rewritten backend.
data/
/amdl-api

test/

/docs/

*.p8
keys/

# Editor / IDE settings.
.idea/
.vscode/

# Go test & coverage artifacts.
*.out
coverage.txt

*.local.yaml
# Live config files: bootstrapped from the example files on first start.
# config.yaml is the owner-edited startup config; runtime.yaml is rewritten
# by PUT /api/v1/config. Neither is tracked, nor is the one-time backup left
# by the legacy single-file migration.
configs/config.yaml
configs/runtime.yaml
configs/config.yaml.pre-split.bak
